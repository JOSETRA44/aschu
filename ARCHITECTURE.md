# Qawaqawa Rural Logistics - Project Structure

## Complete Folder Hierarchy

```
aschu/
│
├── android/                          # Android native configuration
│   ├── app/
│   │   ├── src/
│   │   │   └── main/
│   │   │       └── AndroidManifest.xml  # Add Google Maps API key here
│   │   └── build.gradle.kts
│   ├── build.gradle.kts
│   ├── gradle.properties
│   └── settings.gradle.kts
│
├── ios/                              # iOS native configuration
│   ├── Runner/
│   │   ├── AppDelegate.swift         # Add Google Maps API key here
│   │   └── Info.plist
│   └── Runner.xcodeproj/
│
├── lib/                              # Main application code
│   │
│   ├── core/                         # Core utilities & infrastructure
│   │   │
│   │   ├── constants/
│   │   │   └── map_styles.dart       # Dark GTA minimalist map style JSON
│   │   │
│   │   ├── di/                       # Dependency Injection
│   │   │   ├── injection_container.dart
│   │   │   ├── injection_container.config.dart  # Generated by Injectable
│   │   │   └── register_module.dart   # Injectable modules
│   │   │
│   │   ├── error/                    # Error handling
│   │   │   ├── exceptions.dart       # Exception classes
│   │   │   └── failures.dart         # Failure classes (Dartz)
│   │   │
│   │   └── theme/                    # Theming
│   │       └── app_theme.dart        # AppTheme with ColorScheme
│   │
│   ├── features/                     # Feature modules (Clean Architecture)
│   │   │
│   │   └── map/                      # Map Feature
│   │       │
│   │       ├── data/                 # Data Layer
│   │       │   ├── datasources/
│   │       │   │   ├── map_remote_datasource.dart
│   │       │   │   └── map_remote_datasource_impl.dart
│   │       │   ├── models/
│   │       │   │   └── vehicle_location_model.dart
│   │       │   └── repositories/
│   │       │       └── map_repository_impl.dart
│   │       │
│   │       ├── domain/               # Domain Layer (Business Logic)
│   │       │   ├── entities/
│   │       │   │   └── vehicle_location.dart
│   │       │   ├── repositories/
│   │       │   │   └── map_repository.dart
│   │       │   └── usecases/
│   │       │       ├── get_vehicle_locations.dart
│   │       │       └── watch_vehicle_location.dart
│   │       │
│   │       └── presentation/         # Presentation Layer (UI)
│   │           ├── bloc/
│   │           │   ├── map_bloc.dart
│   │           │   ├── map_event.dart
│   │           │   └── map_state.dart
│   │           ├── pages/
│   │           │   └── map_page.dart
│   │           └── widgets/
│   │               └── custom_map_view.dart
│   │
│   └── main.dart                     # App entry point
│
├── test/                             # Unit & Widget tests
│   └── widget_test.dart
│
├── web/                              # Web configuration
│   ├── index.html
│   └── manifest.json
│
├── windows/                          # Windows desktop configuration
│   └── runner/
│
├── analysis_options.yaml             # Lint rules
├── build.yaml                        # Build runner configuration
├── pubspec.yaml                      # Dependencies
└── README.md                         # Documentation

```

## Layer Responsibilities

### 1. **Domain Layer** (Business Logic)
- **Entities**: Pure business objects (VehicleLocation)
- **Repository Interfaces**: Contracts for data operations
- **Use Cases**: Single-responsibility business operations
- **No dependencies** on other layers

### 2. **Data Layer** (Data Management)
- **Models**: Data transfer objects extending entities
- **Data Sources**: API, database, cache implementations
- **Repository Implementations**: Concrete implementations of domain contracts
- **Depends on**: Domain layer only

### 3. **Presentation Layer** (UI)
- **BLoC**: State management (Events, States, BLoC)
- **Pages**: Screen-level widgets
- **Widgets**: Reusable UI components
- **Depends on**: Domain layer (not Data layer directly)

### 4. **Core** (Shared Infrastructure)
- **DI**: Dependency injection setup
- **Theme**: App-wide styling
- **Constants**: Shared constants
- **Error**: Error handling utilities

## Dependency Flow

```
Presentation ─┐
              ├──> Domain <─── Data
Core ─────────┘
```

## Key Principles

1. **Single Responsibility**: Each class has one job
2. **Dependency Inversion**: Depend on abstractions, not concretions
3. **Separation of Concerns**: UI, business logic, and data are separate
4. **Testability**: Easy to mock and test each layer independently
5. **Scalability**: Easy to add new features without affecting existing code

## File Naming Conventions

- **Entities**: `entity_name.dart` (e.g., `vehicle_location.dart`)
- **Models**: `entity_name_model.dart` (e.g., `vehicle_location_model.dart`)
- **Repositories**: `feature_repository.dart` (e.g., `map_repository.dart`)
- **Use Cases**: `action_description.dart` (e.g., `get_vehicle_locations.dart`)
- **BLoC**: `feature_bloc.dart`, `feature_event.dart`, `feature_state.dart`
- **Pages**: `page_name_page.dart` (e.g., `map_page.dart`)
- **Widgets**: `widget_description.dart` (e.g., `custom_map_view.dart`)

## Next Steps

1. ✅ Add Supabase credentials to `main.dart`
2. ✅ Add Google Maps API keys to Android & iOS
3. ✅ Create Supabase table `vehicle_locations`
4. ✅ Run the app: `flutter run`
5. 🚀 Start building additional features!
